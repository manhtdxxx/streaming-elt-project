version: 2

sources:
  - name: raw_taxi
    description: "taxi_dwh.raw"
    database: taxi_dwh
    schema: raw

    tables:
      - name: green_taxi
        identifier: green_taxi # real table name
        description: "taxi_dwh.raw.green_taxi"
        # config:
        #   loaded_at_field: loaded_at # field in table to check freshness
        #   freshness:
        #     error_after: { count: 24, period: hour }
        #     warn_after: { count: 12, period: hour }

      - name: yellow_taxi
        identifier: yellow_taxi # real table name
        description: "taxi_dwh.raw.yellow_taxi"
        # config:
        #   loaded_at_field: loaded_at # field in table to check freshness
        #   freshness:
        #     error_after: { count: 24, period: hour }
        #     warn_after: { count: 12, period: hour }
